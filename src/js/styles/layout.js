const base = {
  w: 600,
};

export function layout() {
  return {
    width: base.w + 'px',
    marginLeft: 'auto',
    marginRight: 'auto',
    maxWidth: '100%',
    fontFamily: '"Roboto"',
    lineHeight: 1.4,
  };
};
